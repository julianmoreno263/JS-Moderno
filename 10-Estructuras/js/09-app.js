//operador ternario, evalua igual que el if pero la sintaxis es mas simple,si la condicion se cumple se ejecuta el codigo despues del signo ?, si no se cumple se ejecuta el codigo despues de los dos puntos :

//tambien puedo evaluar dos condiciones con el and &&

const autenticado=true
const puedePagar=true

console.log(autenticado && puedePagar ? "Si puede pagar" : "No puede pagar")

//tambien pueden haber ternarios anidados pero no es tan comun