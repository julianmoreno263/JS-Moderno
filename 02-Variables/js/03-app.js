//con const se crean constantes en js, no pueden ser reasignadas
const producto="Monitor"

//las constantes deben de inicializarse de una vez, si solo se crean sin valor sacan un error
const precio=20

console.log(producto)